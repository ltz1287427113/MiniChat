<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".activity.ChatDetailActivity">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:background="?android:attr/windowBackground"
        app:layout_constraintTop_toTopOf="parent"
        app:menu="@menu/chat_detail_menu"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:titleCentered="true"
        app:titleTextColor="@android:color/black" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_message_list"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@+id/input_layout"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/toolbar" />

    <LinearLayout
        android:id="@+id/input_layout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="?android:attr/windowBackground"
        android:orientation="horizontal"
        android:padding="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

<!--android:minHeight="40dp"： (新添加) 确保 EditText 在没内容时，也保持至少 40dp 的高度。-->

<!--android:inputType="textMultiLine"： 替换 text。这是最关键的属性，它告诉 EditText 允许输入多行，并开启垂直滚动功能。-->

<!--android:maxLines="5"： (新添加) 这是“滚动”的关键。它告诉 EditText：“你最多只能长到 5 行的高度。如果超过 5 行，就不要再长高了，开始在内部滚动。”-->

<!--android:scrollbars="vertical"： (新添加) 当内容开始滚动时，显示一个垂直滚动条。-->

<!--android:gravity="top|start"： (新添加) 当 EditText 长高后，让光标和文字从左上角开始，而不是（默认的）垂直居中。-->
        <EditText
            android:id="@+id/et_message_input"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="top|start"
            android:importantForAutofill="no"
            android:inputType="textMultiLine"
            android:maxLines="5"
            android:minHeight="40dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:scrollbars="vertical"
            android:textColor="#333333"
            android:textColorHint="#99A9BF" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_send"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="#3DDC84"
            android:text="发送"
            android:textColor="#344054"
            android:visibility="gone"
            app:cornerRadius="5dp" />

        <ImageButton
            android:id="@+id/btn_more"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="#FFF8FF"
            android:src="@drawable/ic_more" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>